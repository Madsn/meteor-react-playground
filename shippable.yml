build_image: shippableimages/ubuntu1404_nodejs
language: node_js
node_js:
  - 4.2.3
before_install:
  - node --version
  - npm install -g gagarin
install:
  - curl https://install.meteor.com/ | sh
before_script:
  - meteor npm install
script: gagarin -v

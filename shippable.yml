language: node_js
node_js:
  - "6.1.0"
before_install:
  - echo "npm --version"
  - echo "node --version"
  - npm install -g gagarin
install:
  - curl https://install.meteor.com/ | sh
script: gagarin -v
